#ifndef HELP_H_
#define HELP_H_

/* Runs the help UI menu, allowing access to specific help topics. */
void help_run(void);

/* Runs the UI menu for the overview topic. */
void help_overview_run(void);

/* Runs the UI menu for the controls topic. */
void help_controls_run(void);

/* Runs the UI menu for the parameters topic. */
void help_parameters_run(void);

#endif /* HELP_H_ */
